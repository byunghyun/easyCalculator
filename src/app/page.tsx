import RootMenu from '@/components/rootMenu';
import './global.scss';

const Home = () => {
  return (
    <main className='flex w-full h-full'>
      <RootMenu />
    </main>
  );
};

export default Home;
